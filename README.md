# codealpha_tasks
AI Music Generator using LSTM is a deep learning project that learns musical patterns from MIDI files and generates new music compositions. It uses Python, Music21, and TensorFlow to extract notes, train an LSTM neural network, predict note sequences, and create original MIDI music files automatically.
